[package]
name = "rsh-terminal"
description = "Terminal state, modes, ownership, and size for the rsh shell"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
nix = { workspace = true }

[dev-dependencies]
# Only the tests need a pseudoterminal to run against.
nix = { workspace = true }

[lints]
workspace = true

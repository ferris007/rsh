[package]
name = "whelk-parser"
description = "Tokenizer and AST for the whelk shell"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]

[lints]
workspace = true

# No harness: the allocation counter in this test is process-global, so
# anything libtest does on another thread lands in the measurement. A plain
# `main` has one thread and nothing else running in it.
[[test]]
name = "allocations"
harness = false
